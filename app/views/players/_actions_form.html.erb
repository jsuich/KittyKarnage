<!-- # @player_one = 1
# @player_two = 2 -->

<% @moves = 8 %>

<%= form_tag('/JStakesOverThisPart', method: "post") do %>

  <% (@moves).times do |move_num| %>

    <%= label_tag(("#{locals[:player_id]}_move_#{move_num + 1}").to_sym, "move #{move_num + 1} : ") %>
   <%= select_tag(("#{locals[:player_id]}_move_#{move_num + 1}").to_sym, options_for_select(["Walk", "Wait", "Rotate Left", "Rotate Right", "FlameThrower", "Cloak and Dagger", "Lightning Strike", "Drunken Headbutt"], "Walk" ))%>

    <br>
  <% end %>
<% end %>
